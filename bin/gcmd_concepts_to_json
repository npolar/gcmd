#!/usr/bin/env ruby
require "bundler/setup"
require "yajl/json_gem"
require_relative "./../lib/gcmd"

concepts = Gcmd::Concepts.new
docs = []

Gcmd::Concepts.schemas.each do |schema|
  docs += concepts.collection(schema) 
end
puts docs.to_json
